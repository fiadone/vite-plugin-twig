{% extends './base.twig' %}

{% block content %}
  <div class="container">
    <h1>{{ title }}</h1>
    {% for article in articles|default([]) %}
      {% include './fragments/article.twig' with article only %}
    {% endfor %}
  </div>
{% endblock %}
